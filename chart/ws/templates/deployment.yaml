##################################################################################################
# ws services
##################################################################################################
apiVersion: v1
kind: Service
metadata:
  name: {{  .Chart.Name }}
  labels:
    app.kubernetes.io/managed-by: helm
    meta.helm.sh/release-name: {{  .Chart.Name }}
    meta.helm.sh/release-namespace: default
    app: {{  .Chart.Name }}
    service: {{  .Chart.Name }}
spec:
  ports:
  - port: {{ .Values.ws.service.servicePort }}
    name: http
  selector:
    app: {{  .Chart.Name }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{  .Chart.Name }}
  labels:
    app.kubernetes.io/managed-by: helm
    meta.helm.sh/release-name: {{  .Chart.Name }}
    meta.helm.sh/release-namespace: default
    account: {{  .Chart.Name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{  .Chart.Name }}-{{  .Values.ws.version }}
  labels:
    app.kubernetes.io/managed-by: helm
    meta.helm.sh/release-name: {{  .Chart.Name }}
    meta.helm.sh/release-namespace: default
    app: {{  .Chart.Name }}
    version: {{  .Values.ws.version }}
spec:
  replicas: {{  .Values.ws.replicaCount }}
  selector:
    matchLabels:
      app: {{  .Chart.Name }}
      version: {{  .Values.ws.version }}
  template:
    metadata:
      labels:
        app: {{  .Chart.Name }}
        version: {{  .Values.ws.version }}
    spec:
      serviceAccountName: {{  .Chart.Name }}
      containers:
      - name: {{  .Chart.Name }}
        image: {{ .Values.ws.image.repository }}:{{ .Values.ws.image.tag }}
        imagePullPolicy: {{ .Values.ws.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.ws.service.servicePort}}
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      imagePullSecrets:
        - name: {{ .Values.imagepullsecrets.name}}
      volumes:
      - name: tmp
        emptyDir: {}
---